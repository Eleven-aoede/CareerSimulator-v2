# Worklog

## 2026-05-14

- Initialized collaboration memory files.
- Folder was empty at session start.
- Defined the product direction as "career simulation + employment guidance".
- Chose to build "career simulation" first as the MVP.
- Captured first-pass product goal, inputs, outputs, and scope.
- Drafted the first version of `SKILL.md` for the career simulation workflow.
- Added the first end-to-end sample input and sample output for a university counseling center internship role.
- Updated the skill to add user-profile intake and descriptive endings.
- Added the first version of the interactive profile intake flow and question bank.
- Revised the intake flow to prefer open-ended questions over fixed multi-choice blocks.
- Added a friend-like tone requirement for the profile intake phase.
- Fixed the AI guide persona as "Xiaoke" for the simulation experience.
- Updated the skill to make Xiaoke's tone more lively, warm, and responsive.
- Added a new writing rule: keep stories realistic but more vivid and less like a plain work log.
- Added `references/story-node-format.md` to define small-step node structure.
- Added `references/option-generation.md` to define white-label option writing rules.
- Updated `SKILL.md` to require reading the node and option references before story generation.
- Added a rewritten sample simulation for `research intern (data insight)` with smaller steps and white-label choices.
- Updated the node rules and sample so each choice is preceded by a 100-200 character state paragraph.
- Added a new interaction module for the role's most common task, including emotion choice and 1-5 difficulty rating.
- Updated the ending spec to include a light experience-level match analysis with explicit reasons.
- Revised the `research intern (data insight)` sample to include the new task mini-sim and enriched endings.
- Clarified the implementation direction: converge toward a fixed interaction text format that can be embedded and run in HTML.
- Added a no-dependency single-file `index.html` version of the current fixed interaction sample.
- Added a client-side script import interface so uploaded or pasted JSON simulation scripts can replace the built-in sample in `index.html`.
- Switched the runtime import contract from Markdown parsing to JSON scripts generated by the skill.
- Tightened the narrative framing so user-facing copy stays inside the "career travel game" world and avoids technical meta terms.
- Fixed Xiaoke's tone baseline, opening line, and pre-intake transition as stable product-facing copy.
- Fixed only the first profile-intake question; made later intake questions dynamic while keeping the target dimensions stable.
- Rewrote the later intake prompt templates so they match the same warm guided tone as the fixed opening and first question.
- Fixed the post-intake closing copy so Xiaoke ends by confirming understanding and handing off to script generation for the simulator.
- Added explicit activation rules so the loaded skill should immediately begin with Xiaoke's fixed opening instead of a generic assistant prompt.
- Switched the preferred profile-collection path from live AI dialogue to frontend-generated structured `user_profile` JSON, with dialogue intake kept only as a fallback.
- Updated `SKILL.md` so structured `job_input + user_profile` can bypass intake and go straight to story-script generation.
- Added a profile-input builder to `index.html` so the frontend can generate, copy, and download the JSON payload used before story generation.
- Added a clearer simulator-side guard in `index.html` so uploading a profile-input JSON shows a specific error instead of failing like a broken story script.
- Rebuilt `index.html` around the new product flow: homepage, Xiaoke mascot, one-question-per-page collection, local history, and result-page JSON download.
- Added `project-management/HTML_FLOW.md` with the new end-to-end page flow diagram.
- Simplified `SKILL.md` again so skipping profile collection now cleanly falls back to a neutral default user instead of relying on dialogue intake.
- Adjusted the collector flow so job information is gathered before user-profile questions.
- Split the web collector into two clearer phases: job info first, then a dedicated transition screen before profile questions.
- Added a direct-start-simulation entry on the homepage and delete actions for each saved history record.
- Rewrote `SKILL.md` into a pure script-generator spec: removed Xiaoke/persona logic, fixed the output architecture, and narrowed the required references to node rules plus option/effect rules.
