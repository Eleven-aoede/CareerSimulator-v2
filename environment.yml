name: ifi-career-sim
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
    - flask>=3.0
    - flask-cors>=4.0
    - openai>=1.6.0
    - gunicorn>=21.0
    - python-dotenv>=1.0
    - filelock>=3.12
